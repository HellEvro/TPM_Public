# Почему FullAI не открывает сделки (чек-лист)

Если автобот/FullAI давно не открывал сделки (часы подряд), проверьте по пунктам.

## 1. Автобот включён и данные загружены

- В логах `bots.py` при старте должно быть: **`✅ Автобот включен и готов к работе`** (если вы включали его через UI).
- Раз в ~5 минут пишется: **`🎯 AutoBot: ON (интервал: 180s)`** или **`⏹️ AutoBot: OFF`**.
- Проверка сигналов **не запускается**, пока не установлен **`first_round_complete`** (первый раунд загрузки свечей и RSI). В логах не должно быть бесконечного: **`⏳ Ожидание первой загрузки свечей и расчёта RSI`**.

**Вывод:** Если автобот OFF или первая загрузка так и не завершилась (например, из‑за ошибок Bybit 10002) — новых входов не будет.

## 2. Есть ли кандидаты на вход (сигналы)

Каждые `check_interval` секунд (по умолчанию 180) в логах появляется блок:

```
[BOTS] === SIGNALS (filters passed, can enter) ===
[BOTS] LONG: X  SHORT: Y  candidates: Z
[BOTS] Active bots: A/B  slots free: S
```

- Если **LONG: 0, SHORT: 0, candidates: 0** — ни одна монета не прошла фильтры.
- Сразу после этого может быть строка: **`📊 Почему 0 кандидатов: без RSI по ТФ: ..., сигнал WAIT: ..., scope/листинг: ..., фильтры: ..., AI: ...`** — по ней видно, почему все отсеялись (нет RSI по таймфрейму, все в WAIT, AI блокирует и т.д.).

**Вывод:** Нет сделок, потому что нет ни одного кандидата (RSI не в зоне ≤29 / ≥71, или фильтры/AI отсекают всех).

## 3. Лимит активных ботов (max_concurrent)

В том же блоке: **`Active bots: A/B  slots free: S`**. Если **slots free: 0** (уже A = B), новые боты не создаются.

**Вывод:** Все слоты заняты — нужно дождаться закрытия части позиций или поднять `max_concurrent` в конфиге.

## 4. FullAI Adaptive (виртуальная обкатка)

Если включён **FullAI Adaptive** с **«Свечей без сделок» (virtual_success_count) > 0**:

- Сначала открываются только **виртуальные** сделки.
- **Реальные** сделки разрешаются после N успешных виртуальных по монете.

В логах при входе может быть запись о виртуальном входе без реального ордера на бирже.

**Вывод:** Реальные сделки могут появляться редко, пока не наберётся нужное число виртуальных успехов.

## 5. Актуальность данных (ошибки API, таймфрейм)

- Если в логах много **`invalid request... server timestamp or recv_window (ErrCode: 10002)`** — возможны сбои/задержки загрузки свечей и RSI, данные устаревают.
- Таймфрейм системы (например 1m) должен совпадать с ожидаемым; RSI считается по этому таймфрейму.

**Вывод:** При постоянных 10002 обновление RSI может быть нерегулярным, сигналы не появляются или «опаздывают».

## 6. Что сделать прямо сейчас

1. Убедиться, что **Auto Bot включён** в UI (переключатель «Auto Bot (ВКЛ)»).
2. Открыть логи **bots.py** и найти последние блоки **`[BOTS] === SIGNALS`** и **`Почему 0 кандидатов`** — по ним понять: 0 кандидатов из‑за RSI/фильтров/AI или из‑за слотов.
3. Проверить **конфиг**: пороги RSI (LONG ≤, SHORT ≥), лимит одновременных ботов, при необходимости — настройки FullAI Adaptive.
4. Если были ошибки **10002** — синхронизировать время с интернетом (NTP) на машине, где запущен `bots.py`, или увеличить `recv_window` в коде биржи.

После проверки по пунктам станет ясно, чего не хватает для появления новых сделок FullAI.
