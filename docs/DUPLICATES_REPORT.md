# Отчет о дублирующихся функциях и функционале

## 🔍 Обнаруженные дубликаты

### 1. Функции расчета RSI (calculate_rsi)

**Дубликаты найдены в:**
- `bot_engine/utils/rsi_utils.py` (строки 6-49)
- `bots_modules/calculations.py` (строки 40-83)
- `utils/rsi_calculator.py` (строки 5-30) - **отличается реализацией** (использует `deltas` вместо `changes`)

**Проблема:** Одинаковая логика расчета RSI дублируется в 3 местах. Функция в `utils/rsi_calculator.py` имеет небольшие отличия в реализации.

**Рекомендация:** Использовать единую функцию из `bot_engine/utils/rsi_utils.py` как основную, остальные заменить на импорт.

---

### 2. Функции расчета истории RSI (calculate_rsi_history)

**Дубликаты найдены в:**
- `bot_engine/utils/rsi_utils.py` (строки 52-96)
- `bots_modules/calculations.py` (строки 85-125+)

**Проблема:** Полностью идентичная логика расчета истории RSI.

**Рекомендация:** Использовать функцию из `bot_engine/utils/rsi_utils.py`, в `bots_modules/calculations.py` заменить на импорт.

---

### 3. Функции расчета EMA (calculate_ema)

**Дубликаты найдены в:**
- `bot_engine/utils/rsi_utils.py` - **НЕ НАЙДЕНА** (только RSI функции)
- `bots_modules/calculations.py` (строки 266-280) - возвращает одно значение
- `bots_modules/imports_and_globals.py` (строки 89-103) - возвращает одно значение
- `bots_modules/filters.py` (строки 70-71, 77-100) - `calculate_ema_list` возвращает список
- `bot_engine/indicators.py` (строки 61-85) - метод класса `TechnicalIndicators`
- `utils/rsi_calculator.py` (строки 32-49) - возвращает список значений
- `scripts/ai/train_lstm_predictor.py` (строка 24) - для обучения LSTM

**Проблема:** 
- Разные реализации: одни возвращают одно значение, другие - список
- `calculate_ema_list` в `filters.py` - отдельная функция для списка значений
- `utils/rsi_calculator.py` возвращает список, а `calculations.py` - одно значение

**Рекомендация:** 
- Унифицировать: создать базовую функцию `calculate_ema` в `bot_engine/utils/` или `bot_engine/indicators.py`
- Добавить опциональный параметр `return_list=False` для возврата списка или одного значения
- Заменить все дубликаты на импорт

---

### 4. Функции загрузки/сохранения RSI кэша

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_rsi_cache(coins_data, stats)` (строки 111-118)
  - `load_rsi_cache()` (строки 121-142)
- `bots_modules/sync_and_cache.py`:
  - `save_rsi_cache()` (строки 235-255) - без параметров, использует глобальную переменную
  - `load_rsi_cache()` (строки 257-290+) - без параметров, использует глобальную переменную

**Проблема:** 
- Разные сигнатуры: `storage.py` принимает параметры, `sync_and_cache.py` работает с глобальными переменными
- Разная логика работы с данными

**Рекомендация:** 
- Унифицировать подход: использовать функции из `bot_engine/storage.py` как базовые
- В `sync_and_cache.py` обернуть вызовы функций из `storage.py` для работы с глобальными переменными

---

### 5. Функции загрузки/сохранения состояния ботов

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_bots_state(bots_data, auto_bot_config)` (строки 159-170)
  - `load_bots_state()` (строки 173-175)
- `bots_modules/sync_and_cache.py`:
  - `save_bots_state()` (строки 535-575) - без параметров, использует глобальные переменные
  - `load_bots_state()` (строки 661-722) - без параметров, использует глобальные переменные
- `bots_modules/init_functions.py` (строки 96-98) - локальные функции-обертки
- `bots_modules/workers.py` (строка 52) - локальная функция-обертка
- `bots_modules/api_endpoints.py` (строка 82) - локальная функция-обертка

**Проблема:** Множественные обертки вокруг базовых функций из `storage.py`.

**Рекомендация:** Использовать функции из `bot_engine/storage.py` напрямую, убрать лишние обертки.

---

### 6. Функции работы с зрелыми монетами (mature coins)

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_mature_coins(storage)` (строки 240-245)
  - `load_mature_coins()` (строки 248-253)
- `bot_engine/maturity_checker.py`:
  - `get_mature_coins_storage()` (строка 216)
  - `set_mature_coins_storage(new_storage)` (строка 222)
- `bots_modules/maturity.py`:
  - `load_mature_coins_storage(expected_coins_count=None)` (строки 80-159)
  - `save_mature_coins_storage()` (строки 160-200+)
- `bots_modules/init_functions.py` (строка 88) - локальная функция-обертка
- `bots_modules/workers.py` (строка 54) - локальная функция-обертка

**Проблема:** Разные функции для работы с одними и теми же данными в разных модулях.

**Рекомендация:** Унифицировать: использовать функции из `bot_engine/storage.py` для сохранения/загрузки, `maturity_checker.py` для логики проверки.

---

### 7. Функция get_effective_signal

**Дубликаты найдены в:**
- `bot_engine/signal_processor.py` (строки 10-58) - принимает `coin, config`
- `bots_modules/filters.py` (строки 1240-1300+) - принимает только `coin`, читает конфиг из глобальной переменной
- `bots_modules/api_endpoints.py` (строка 84) - локальная функция внутри endpoint

**Проблема:** 
- Разные сигнатуры
- Разная логика работы с конфигурацией
- Локальная функция в `api_endpoints.py` - явное дублирование

**Рекомендация:** 
- Использовать функцию из `bot_engine/signal_processor.py` как основную
- В `bots_modules/filters.py` обернуть для работы с глобальным конфигом
- Удалить локальную функцию из `api_endpoints.py`

---

### 8. Функции проверки зрелости монет (check_coin_maturity)

**Дубликаты найдены в:**
- `bot_engine/maturity_checker.py` (строки 24-100+) - основная функция
- `bots_modules/maturity.py` (строки 172-300+) - функция с хранилищем
- `bots_modules/calculations.py` (строка 172) - функция проверки зрелости

**Проблема:** Разные реализации логики проверки зрелости.

**Рекомендация:** Унифицировать логику, использовать `bot_engine/maturity_checker.py` как базовую реализацию.

---

### 9. Функции загрузки/сохранения конфигурации автобота

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_auto_bot_config(config)` (строки 179-184)
  - `load_auto_bot_config()` (строки 187-189)
- `bots_modules/imports_and_globals.py`:
  - `load_auto_bot_config()` (строки 564-583)
  - `get_auto_bot_config()` (строки 584-599)
- `bots_modules/sync_and_cache.py`:
  - `save_default_config()` (строки 307-319)
  - `load_default_config()` (строки 320-361)

**Проблема:** Разные функции для работы с конфигурацией автобота.

**Рекомендация:** Использовать функции из `bot_engine/storage.py`, остальные заменить на импорты или обертки.

---

### 10. Функции загрузки/сохранения системной конфигурации

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_system_config(config)` (строки 284-289)
  - `load_system_config()` (строки 292-294)
- `bots_modules/sync_and_cache.py`:
  - `save_system_config(config_data)` (строки 419-431)
  - `load_system_config()` (строки 432-534)

**Проблема:** Дублирование функций сохранения/загрузки системной конфигурации.

**Рекомендация:** Использовать функции из `bot_engine/storage.py`, в `sync_and_cache.py` заменить на импорт.

---

### 11. Функции загрузки/сохранения состояния процессов

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_process_state(process_state)` (строки 267-274)
  - `load_process_state()` (строки 277-280)
- `bots_modules/sync_and_cache.py`:
  - `save_process_state()` (строки 374-391) - без параметров
  - `load_process_state()` (строки 392-418) - без параметров

**Проблема:** Разные сигнатуры функций.

**Рекомендация:** Унифицировать: использовать функции из `bot_engine/storage.py` как базовые.

---

### 12. Функции загрузки/сохранения индивидуальных настроек монет

**Дубликаты найдены в:**
- `bot_engine/storage.py`:
  - `save_individual_coin_settings(settings)` (строки 193-220)
  - `load_individual_coin_settings()` (строки 223-236)
- `bots_modules/imports_and_globals.py`:
  - `load_individual_coin_settings()` (строки 600-620)
  - `save_individual_coin_settings(symbol, settings, persist=True)` (строки 621-635)
  - `get_individual_coin_settings(symbol)` (строки 636-645)
  - `set_individual_coin_settings(symbol, settings, persist=True)` (строки 646-655)

**Проблема:** Разные функции для работы с индивидуальными настройками монет.

**Рекомендация:** Использовать функции из `bot_engine/storage.py` для базовых операций, добавить функции-обертки в `imports_and_globals.py` для работы с отдельными монетами.

---

## 📊 Статистика дублирования

- **RSI функции:** 3 дубликата
- **EMA функции:** 7+ дубликатов (разные реализации)
- **Storage функции:** 10+ дубликатов
- **Maturity функции:** 5+ дубликатов
- **Signal processing:** 3 дубликата

**Всего найдено:** ~30+ дублирующихся функций

---

## 🎯 Приоритеты рефакторинга

### Высокий приоритет:
1. **RSI функции** - критично для корректности расчетов
2. **Storage функции** - влияют на целостность данных
3. **get_effective_signal** - используется в торговой логике

### Средний приоритет:
4. **EMA функции** - нужно унифицировать возвращаемые значения
5. **Maturity функции** - влияют на фильтрацию монет

### Низкий приоритет:
6. **Config функции** - обертки можно оставить для удобства

---

## 💡 Рекомендации по рефакторингу

1. **Создать единую точку входа для всех расчетов:**
   - Использовать `bot_engine/utils/rsi_utils.py` для RSI
   - Использовать `bot_engine/indicators.py` для EMA и других индикаторов

2. **Унифицировать storage функции:**
   - Использовать `bot_engine/storage.py` как единый модуль для всех операций с файлами
   - Остальные модули должны импортировать и использовать эти функции

3. **Убрать локальные дубликаты:**
   - Удалить локальные функции из `api_endpoints.py`, `workers.py`, `init_functions.py`
   - Заменить на импорты из базовых модулей

4. **Добавить документацию:**
   - Указать в комментариях, какие функции являются основными
   - Указать, какие функции являются обертками

