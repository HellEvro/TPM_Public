# Тестирование стоп-лоссов через API

## Описание

Скрипты для проверки работы функционала стоп-лоссов через API биржи.

## Файлы

### 1. `test_stop_loss_api_safe.py` ⭐ РЕКОМЕНДУЕТСЯ НАЧАТЬ С ЭТОГО

**Безопасная версия - ТОЛЬКО ЧТЕНИЕ**

- ✅ Не изменяет реальные стоп-лоссы
- ✅ Проверяет инициализацию exchange
- ✅ Получает и анализирует позиции
- ✅ Проверяет наличие стоп-лоссов
- ✅ Рассчитывает стоп-лоссы (без установки)
- ✅ Проверяет доступность методов API

**Использование:**
```bash
python scripts/test_stop_loss_api_safe.py
```

### 2. `test_stop_loss_api.py` ⚠️ УСТАНАВЛИВАЕТ РЕАЛЬНЫЕ СТОП-ЛОССЫ

**Полная версия - УСТАНАВЛИВАЕТ СТОП-ЛОССЫ**

- ⚠️ **УСТАНАВЛИВАЕТ РЕАЛЬНЫЕ СТОП-ЛОССЫ НА БИРЖЕ!**
- ✅ Проверяет инициализацию exchange
- ✅ Получает позиции
- ✅ Устанавливает стоп-лоссы (5%)
- ✅ Устанавливает трейлинг стопы (если есть прибыль >= 3%)
- ✅ Вызывает функцию `check_missing_stop_losses()`

**Использование:**
```bash
python scripts/test_stop_loss_api.py
```

⚠️ **ВНИМАНИЕ:** Этот скрипт будет устанавливать реальные стоп-лоссы на ваших позициях!

## Что проверяют тесты

### Тест 1: Инициализация Exchange
- Создание exchange через `ExchangeFactory`
- Установка в `GlobalState` через `set_exchange()`
- Получение через `get_exchange()`
- Проверка через `ensure_exchange_initialized()`
- Тест подключения к бирже

### Тест 2: Получение позиций
- Получение позиций через API
- Анализ активных позиций
- Проверка наличия стоп-лоссов
- Проверка наличия трейлинг стопов
- Расчет прибыли/убытка

### Тест 3: Установка стоп-лосса (только в полной версии)
- Расчет стоп-лосса (5% от цены входа)
- Установка через `set_trading_stop()`
- Проверка установки на бирже

### Тест 4: Установка трейлинг стопа (только в полной версии)
- Проверка прибыли (>= 3%)
- Установка трейлинг стопа (1.5%)
- Проверка установки на бирже

### Тест 5: Проверка функции `check_missing_stop_losses()`
- Вызов основной функции
- Проверка работы всей логики

## Рекомендуемый порядок тестирования

1. **Сначала запустите безопасную версию:**
   ```bash
   python scripts/test_stop_loss_api_safe.py
   ```
   
   Это покажет:
   - Работает ли подключение к бирже
   - Есть ли открытые позиции
   - У каких позиций нет стоп-лоссов
   - Правильно ли рассчитываются стоп-лоссы

2. **Если все работает, запустите полную версию:**
   ```bash
   python scripts/test_stop_loss_api.py
   ```
   
   Это установит стоп-лоссы на позициях без них.

## Интерпретация результатов

### ✅ Все тесты прошли
- Exchange работает корректно
- API биржи доступен
- Стоп-лоссы можно устанавливать

### ❌ Ошибки инициализации
- Проверьте ключи API в `app/config.py`
- Проверьте подключение к интернету
- Проверьте доступность биржи

### ❌ Ошибки получения позиций
- Проверьте права API ключей
- Проверьте, что есть открытые позиции
- Проверьте логи ошибок

### ❌ Ошибки установки стоп-лоссов
- Проверьте параметры позиции (positionIdx)
- Проверьте права API (нужны права на торговлю)
- Проверьте код ошибки в логах

## Коды ошибок API

### retCode = 0
✅ Успешно

### retCode = 34040
ℹ️ "Not modified" - стоп-лосс уже установлен на эту цену (это нормально)

### Другие коды
❌ Ошибка - см. `retMsg` в логах

## Примеры использования

### Только проверка (безопасно)
```bash
python scripts/test_stop_loss_api_safe.py
```

### Полное тестирование с установкой стоп-лоссов
```bash
python scripts/test_stop_loss_api.py
```

### Проверка конкретной функции
```python
from bots_modules.sync_and_cache import check_missing_stop_losses
result = check_missing_stop_losses()
print(f"Результат: {result}")
```

## Логи

Все тесты выводят подробные логи:
- ✅ Успешные операции
- ❌ Ошибки
- ⚠️ Предупреждения
- 📊 Информация о позициях
- 💡 Подсказки

## Устранение проблем

### Exchange не инициализирован
1. Проверьте `app/config.py` - правильные ли ключи API
2. Проверьте подключение к интернету
3. Проверьте, что биржа доступна

### Позиции не найдены
- Это нормально, если у вас нет открытых позиций
- Тесты установки стоп-лоссов будут пропущены

### Ошибка установки стоп-лосса
1. Проверьте права API ключей (нужны права на торговлю)
2. Проверьте режим позиций (Hedge/One-way)
3. Проверьте `positionIdx` для вашего режима

## Безопасность

- ⚠️ **Полная версия скрипта изменяет реальные стоп-лоссы на бирже!**
- ✅ Всегда сначала запускайте безопасную версию
- ✅ Проверяйте логи перед установкой стоп-лоссов
- ✅ Убедитесь, что понимаете, что делает скрипт

