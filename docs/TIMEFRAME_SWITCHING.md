# Переключение таймфреймов системы

## Обзор

Система поддерживает динамическое переключение таймфреймов на лету без перезапуска. При переключении таймфрейма все компоненты системы автоматически адаптируются к новому таймфрейму:
- Расчет RSI и тренда
- Обучение AI
- Отображение интерфейса
- Хранение данных в БД
- API endpoints

## Поддерживаемые таймфреймы

- `1m` - 1 минута
- `3m` - 3 минуты
- `5m` - 5 минут
- `15m` - 15 минут
- `30m` - 30 минут
- `1h` - 1 час
- `2h` - 2 часа
- `4h` - 4 часа
- `6h` - 6 часов (по умолчанию)
- `8h` - 8 часов
- `12h` - 12 часов
- `1d` - 1 день
- `3d` - 3 дня
- `1w` - 1 неделя
- `1M` - 1 месяц

## Как переключить таймфрейм

### Через интерфейс (рекомендуется)

1. Откройте страницу **Боты** в веб-интерфейсе
2. Перейдите на вкладку **Конфигурация**
3. Найдите раздел **⚙️ Системные настройки**
4. В блоке **⏱️ Таймфрейм системы** выберите нужный таймфрейм из выпадающего списка
5. Нажмите кнопку **✅ Применить таймфрейм**

Система автоматически:
- Сохранит текущие данные
- Очистит кэш свечей
- Перезагрузит RSI данные с новым таймфреймом
- Обновит интерфейс
- Продолжит работу в новом таймфрейме

### Программно (Python)

```python
from bot_engine.bot_config import set_current_timeframe, get_current_timeframe

# Получить текущий таймфрейм
current_tf = get_current_timeframe()
print(f"Текущий таймфрейм: {current_tf}")

# Установить новый таймфрейм
success = set_current_timeframe('4h')
if success:
    print("Таймфрейм успешно изменен на 4h")
else:
    print("Ошибка: неподдерживаемый таймфрейм")
```

### Через API

```bash
# Получить текущий таймфрейм
curl http://localhost:5001/api/bots/timeframe

# Установить новый таймфрейм
curl -X POST http://localhost:5001/api/bots/timeframe \
  -H "Content-Type: application/json" \
  -d '{"timeframe": "4h"}'
```

## Архитектура

### Централизованная система управления

Все компоненты системы используют функции из `bot_engine/bot_config.py`:

- `get_current_timeframe()` - получение текущего таймфрейма
- `set_current_timeframe(timeframe)` - установка таймфрейма в runtime
- `get_rsi_key(timeframe)` - получение ключа для RSI данных (например, 'rsi6h', 'rsi4h')
- `get_trend_key(timeframe)` - получение ключа для тренда (например, 'trend6h', 'trend4h')
- `get_rsi_from_coin_data(coin_data, timeframe)` - получение RSI с обратной совместимостью
- `get_trend_from_coin_data(coin_data, timeframe)` - получение тренда с обратной совместимостью

### База данных

База данных автоматически мигрируется при переключении таймфрейма:
- При первом использовании нового таймфрейма автоматически добавляются колонки `rsi{timeframe}` и `trend{timeframe}`
- Старые данные сохраняются в колонках `rsi6h` и `trend6h` для обратной совместимости
- При загрузке данных система использует колонки текущего таймфрейма, если они существуют

### Обратная совместимость

Система полностью обратно совместима:
- Старые данные с ключами `rsi6h`/`trend6h` продолжают работать
- Функции `get_rsi_from_coin_data()` и `get_trend_from_coin_data()` автоматически используют правильные ключи
- При отсутствии данных для текущего таймфрейма используются данные для 6h

## Что происходит при переключении

1. **Сохранение данных**: Текущие RSI данные сохраняются в БД
2. **Очистка кэша**: Кэш свечей и RSI данных очищается
3. **Установка нового таймфрейма**: Глобальная переменная `_current_timeframe` обновляется
4. **Перезагрузка данных**: В фоновом потоке запускается перезагрузка RSI данных с новым таймфреймом
5. **Обновление интерфейса**: Все упоминания таймфрейма в UI обновляются
6. **Продолжение работы**: Система продолжает работу с новым таймфреймом

## Важные замечания

1. **Обучение AI**: При переключении таймфрейма AI будет обучаться на данных нового таймфрейма. Рекомендуется переобучить модели после переключения.

2. **Исторические данные**: Данные для нового таймфрейма будут собираться с момента переключения. Для полной истории может потребоваться время.

3. **Производительность**: Переключение таймфрейма может временно увеличить нагрузку на систему из-за перезагрузки данных.

4. **Активные боты**: Активные боты продолжают работать с данными, которые были доступны на момент их создания. Новые боты будут использовать новый таймфрейм.

## Технические детали

### Структура данных

Данные монеты могут содержать RSI и тренд для нескольких таймфреймов одновременно:

```python
coin_data = {
    'symbol': 'BTCUSDT',
    'rsi6h': 45.2,      # Старый таймфрейм (для совместимости)
    'trend6h': 'UP',
    'rsi4h': 47.1,      # Новый таймфрейм
    'trend4h': 'UP',
    'price': 50000,
    # ...
}
```

### Использование в коде

Всегда используйте функции-хелперы вместо прямого доступа к ключам:

```python
# ✅ Правильно
from bot_engine.bot_config import get_rsi_from_coin_data, get_trend_from_coin_data
rsi = get_rsi_from_coin_data(coin_data)
trend = get_trend_from_coin_data(coin_data)

# ❌ Неправильно
rsi = coin_data.get('rsi6h')  # Жестко закодированный таймфрейм
```

### В JavaScript

В интерфейсе также используется динамический таймфрейм:

```javascript
// Получение текущего таймфрейма
const currentTimeframe = document.getElementById('systemTimeframe')?.value || '6h';
const rsiKey = `rsi${currentTimeframe}`;
const trendKey = `trend${currentTimeframe}`;

// Использование
const rsi = coin[rsiKey] || coin.rsi6h || coin.rsi || 50;
const trend = coin[trendKey] || coin.trend6h || coin.trend || 'NEUTRAL';
```

## Миграция существующих данных

При первом переключении на новый таймфрейм система автоматически:
1. Создаст необходимые колонки в БД
2. Начнет собирать данные для нового таймфрейма
3. Сохранит старые данные для обратной совместимости

Для полной миграции всех исторических данных может потребоваться дополнительный скрипт (если необходимо).

## Отладка

Для проверки текущего таймфрейма:

```python
from bot_engine.bot_config import get_current_timeframe
print(f"Текущий таймфрейм: {get_current_timeframe()}")
```

В логах система выводит информацию о переключении таймфрейма:
```
🔄 Таймфрейм изменен: 6h → 4h
🗑️ Кэш свечей очищен для перезагрузки с новым таймфреймом
🔄 Запуск перезагрузки RSI данных для таймфрейма 4h...
✅ RSI данные перезагружены для таймфрейма 4h
```
