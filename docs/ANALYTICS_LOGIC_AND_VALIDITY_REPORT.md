# Аналитика логики, валидности и эффективности InfoBot

Отчёт о перекрёстной проверке сценариев, конфига, логов и БД.

---

## 1. Матрица сценариев входа

| Конфиг | full_ai_control | ai_enabled | AI модули | AI проверка в filters | Источник решения | ai_decision_id | Самообучение при закрытии |
|--------|-----------------|------------|-----------|------------------------|------------------|----------------|---------------------------|
| Только скрипт | ❌ | ❌ | ❌ | Нет | SCRIPT | — | — |
| AI рекомендательный | ❌ | ✅ | ✅ | should_open_position_with_ai | AI/SCRIPT | Да (если AI) | update_ai_decision_result |
| FullAI | ✅ | — | ✅ | get_ai_entry_decision | AI | **Нет** | process_trade_for_self_learning (через ветку decision_source==AI) |
| FullAI Adaptive (virtual) | ✅ | — | ✅ | get_ai_entry_decision | AI | Нет | — (виртуальные — в fullai_adaptive) |

**Ключевой момент:** При FullAI `get_ai_entry_decision` не возвращает `ai_decision_id`, но `_last_decision_source='AI'` сохраняется. Самообучение вызывается из `_log_position_closed` при `decision_source == 'AI'`.

---

## 2. Матрица сценариев закрытия

| Кто обнаружил закрытие | already_closed_trade | effective_reason из bots_db | Действия |
|------------------------|----------------------|-----------------------------|----------|
| **Бот** (_close_position_on_exchange) | — | — | _log_position_closed → bot_history, bots_db, record_real_close, process_trade_for_self_learning |
| **Sync** (позиция исчезла) | False | CLOSED_ON_EXCHANGE | history_log_position_closed, save bots_db, record_real_close |
| **Sync** (позиция исчезла) | False | RSI_EXIT/BOT_LIMIT_TP/SL (найдено в bots_db) | Пропуск дубликатов: не логируем, не сохраняем, не record_real_close |
| **Sync** (позиция исчезла) | True | — | Только удаление бота из памяти |

**Проверка дубликатов:** `_check_if_trade_already_closed` сравнивает `entry_price` (точность 0.0001) и `entry_timestamp` (окно ±2 мин). При расхождении (timezone, точность) возможен двойной учёт.

**Запрос effective_reason:** Окно 5 минут назад. Если бот сохраняет позже — sync может не найти запись и поставить CLOSED_ON_EXCHANGE.

---

## 3. Потоки записи в хранилища

```
Закрытие позиции
    │
    ├─► bot_history_manager.log_position_closed  →  bot_history.json, ai_data.db (ai_db)
    ├─► bots_db.save_bot_trade_history           →  bots_data.db (bot_trades_history)
    ├─► record_real_close                        →  fullai_analytics (fullai_events)
    └─► process_trade_for_self_learning          →  ai_self_learning (online_learning_buffer, state)
```

**Источники вызова:**
- **bot_class._log_position_closed** — когда бот сам закрыл
- **sync_and_cache** — когда sync обнаружил исчезновение позиции
- **api_endpoints** — при ручном закрытии через UI (MANUAL_CLOSE_UI)

---

## 4. Выявленные риски и несоответствия

### 4.1. virtual_only в filters — возможный баг

**Ситуация:** При FullAI Adaptive, если AI даёт WAIT, ставится `last_ai_result['virtual_only'] = True` и монета попадает в `potential_coins`.

**Проблема:** В цикле создания ботов (`process_auto_bot_signals`) нет проверки `virtual_only`. Вызывается `enter_position(direction, force_market_entry=True)` → **открывается реальная позиция вместо виртуальной**.

**Где ожидается виртуальный вход:** В `bot_class.should_open_long` при `get_next_action() == 'virtual_open'` вызывается `record_virtual_open` и возвращается False (реального входа нет). Но `process_auto_bot_signals` создаёт бота напрямую и не использует `should_open_long` — он обходит эту логику.

**Рекомендация:** В `process_auto_bot_signals` перед `enter_position` добавить:
```python
if last_ai_result and last_ai_result.get('virtual_only'):
    from bots_modules.fullai_adaptive import record_virtual_open
    record_virtual_open(symbol, direction, float(coin.get('price') or coin.get('coin_data', {}).get('price') or 0))
    continue
```

### 4.2. Дублирование при быстром sync

**Сценарий:** Бот закрывает позицию и сохраняет в bots_db. Sync запускается сразу после и ещё не видит запись (задержка, другая транзакция).

**Результат:** `_check_if_trade_already_closed` → False, `effective_reason` из recent trades не находится. Sync пишет CLOSED_ON_EXCHANGE и создаёт вторую запись в bots_db (если `skip_save` не сработал).

**Смягчение:** Окно `get_bot_trades_history(from_ts_sec=now-300)` даёт 5 минут. При нормальной скорости это должно покрывать гонку. `_check_if_trade_already_closed` не использует `from_ts_sec`, поэтому может не видеть только что сохранённую сделку.

### 4.3. Совпадение entry_price и entry_timestamp

**Проблема:** `_check_if_trade_already_closed` требует `price_match` (Δ < 0.0001) и `timestamp_match` (±2 мин). При проскальзывании или разнице во времени входа возможны ложные срабатывания или пропуски дубликатов.

**Рекомендация:** Добавить fallback: если по symbol есть недавняя (например, последние 5 мин) закрытая сделка — считать дубликатом даже при небольшом расхождении entry_price.

### 4.4. Источники decision_source

- **bot_class:** `_last_decision_source` из `_set_decision_source`
- **sync trade_data:** `bot_data.get("decision_source", "SCRIPT")` — в `bot_data` может не быть поля `decision_source`, будет "SCRIPT" по умолчанию
- **fullai_analytics:** Не хранит decision_source напрямую; реальный/виртуальный — через event_type

**Рекомендация:** Явно сохранять `decision_source` в bots_data при создании/обновлении бота, чтобы sync всегда брал корректное значение.

---

## 5. Соответствие конфигу и отчётам

### 5.1. fullai_config.json

| Параметр | Влияние | Проверка в коде |
|----------|---------|-----------------|
| fullai_adaptive_enabled | Виртуальная обкатка | is_adaptive_enabled() |
| fullai_adaptive_virtual_success_count | Успешных виртуальных → 1 реальная | get_next_action, _reset_to_virtual |
| take_profit_percent, max_loss_percent | TP/SL, close_source | sync: сравнение exit_price с bot_tp/bot_sl |
| ai_min_confidence | Порог голосования | get_ai_entry_decision |
| ai_override_original | AI блокирует vs рекомендательный | filters: ai_override |

### 5.2. FullAI Analytics (fullai_events)

- **real_open / virtual_open** — из bot_class и fullai_adaptive
- **real_close / virtual_close** — из record_real_close, fullai_adaptive
- **blocked / refused** — из bot_class при блокировках

**Соответствие:** События отражают реальные действия. При рассинхроне sync/бот возможно дублирование или пропуск real_close.

### 5.3. Self-learning (ai_self_learning)

- **До исправления:** Данные шли только при `ai_decision_id` (старый путь с should_open_position_with_ai)
- **После исправления:** FullAI-сделки с `decision_source == 'AI'` тоже идут в `process_trade_for_self_learning`

**Проверка:** В логах при закрытии FullAI-сделки не должно быть исключений из `process_trade_for_self_learning`.

---

## 6. Оценка эффективности

### 6.1. Плюсы текущей логики

1. Разделение: FullAI — `get_ai_entry_decision`, не-FullAI — `should_open_position_with_ai`
2. Учёт причины закрытия: sync берёт effective_reason из bots_db
3. Самообучение получает FullAI-сделки через ветку decision_source
4. Проверка дубликатов через `_check_if_trade_already_closed`
5. transformer_predictor проверяется только при AI_USE_TRANSFORMER

### 6.2. Уязвимые места

1. **virtual_only в filters** — риск реального входа вместо виртуального
2. **Гонка sync/бот** — возможны двойные записи при быстром закрытии
3. **Нет from_ts в _check_if_trade_already_closed** — проверка по «последние 10» без фильтра по времени
4. **decision_source в sync** — берётся из bot_data, поле может отсутствовать

---

## 7. Рекомендации

### Высокий приоритет ✅
1. **virtual_only:** В `process_auto_bot_signals` при `virtual_only` вызывается `record_virtual_open` и `continue` — `enter_position` не вызывается.
2. **Проверка дубликатов:** В `_check_if_trade_already_closed` добавлен фильтр `from_ts_sec`/`to_ts_sec` (окно 10 минут) для надёжности.

### Средний приоритет ✅
3. **decision_source:** `decision_source` сохраняется в `bot_data` через `NewTradingBot.to_dict()`.
4. **Логирование:** Добавлен лог `[SYNC] Используем причину из bots_db: {effective_reason}` при найденной причине.

### Низкий приоритет ✅
5. **Метрики:** Метрики `_sync_closes_total`, `_sync_closes_from_bots_db`; доля логируется при использовании причины из bots_db.
6. **Тесты:** Unit-тесты в `tests/test_analytics_recommendations.py` (virtual_only, _check_if_trade_already_closed, decision_source).

---

## 8. Чек-лист для верификации по логам и БД

- [ ] В логах при FullAI: `🤖 AI разрешает вход` или `→ виртуальная сделка для обучения`
- [ ] При закрытии: нет дублей в `bot_trades_history` по одной позиции (symbol, entry_price, entry_time)
- [ ] В fullai_events: real_close с close_source BOT_LIMIT_TP/SL/RSI_EXIT, а не только CLOSED_ON_EXCHANGE
- [ ] В self_learning_state: растут total_trades_processed и buffer_size
- [ ] При virtual_only: в логах `виртуальная сделка`, в fullai_events — virtual_open, без реального ордера на бирже
