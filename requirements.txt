# =============================================================================
# InfoBot требует Python 3.14.
# Установка: https://www.python.org/downloads/
# Или: python scripts/setup_python_gpu.py (создаёт .venv_gpu с Python 3.14)
#
# pip install -r requirements.txt --no-warn-script-location  — меньше предупреждений PATH
# =============================================================================

# Core dependencies
flask>=2.3.3
flask-cors>=4.0.0
flask-socketio>=5.3.6
python-socketio>=5.9.0
python-engineio>=4.7.1

# Exchange APIs
ccxt>=4.1.13
bybit-api
pybit>=5.11.0
python-binance>=1.0.29
binance-connector>=3.0.0
okx-api>=0.0.1

# Utilities
requests>=2.31.0
python-telegram-bot>=20.6
websocket-client>=1.6.3
python-dotenv>=1.0.0
APScheduler>=3.10.4
psutil>=5.9.0

# Data processing
pandas>=2.1.1
numpy>=1.26.0
matplotlib>=3.7.0
pytz>=2023.3.post1

# Database (SQLite встроен в Python, но может потребоваться для расширенных функций)
# sqlite3 встроен в Python 3.x, дополнительные зависимости не требуются
# Для работы с БД используется стандартный модуль sqlite3

# Async support
aiohttp>=3.8.6
asyncio>=3.4.3

# Logging and formatting
colorama>=0.4.6
rich>=13.0.0

# Security
cryptography>=41.0.0

# AI/ML dependencies
# ВНИМАНИЕ: TensorFlow НЕ поддерживает Python 3.14!
# TensorFlow устанавливается только в .venv_gpu с Python 3.12
# При запуске ai.py выполняется проверка и при необходимости установка
# TensorFlow устанавливается автоматически через bot_engine/ai/tensorflow_setup.py
# Для установки TensorFlow используйте: python scripts/setup_python_gpu.py

# Machine Learning libraries
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0

# Deep Learning (LSTM/CNN) — tensorflow уже выше

# Для обработки изображений (Pattern Recognition)
# opencv-python>=4.8.0  # Обработка изображений для CNN

# Для защиты модулей (только для сборки)
# Cython>=3.0.0        # Компиляция Python в .pyd/.so
# pyarmor>=8.0.0       # Обфускация кода

# Для анализа и визуализации (опционально)
# matplotlib>=3.7.0    # Визуализация графиков (уже указан выше)
# seaborn>=0.12.0      # Статистическая визуализация

# ===========================================
# Примечания:
# ===========================================
# 
# 1. TensorFlow: ~500 MB. GPU: pip install tensorflow[and-cuda]
#
# 2. Для GPU:
#    Начиная с TensorFlow 2.10+ пакет tensorflow-gpu устарел
#    Используйте: pip install tensorflow[and-cuda]
#    Или просто: pip install tensorflow (автоматически использует GPU если доступен)
#    CUDA библиотеки устанавливаются автоматически через pip
#    Требуется NVIDIA GPU с драйверами (проверьте: nvidia-smi)
#
# 3. Минимальные требования для Anomaly Detection:
#    scikit-learn + numpy + pandas + joblib + scipy
#
# 4. Проект требует Python 3.14. Остальные версии не поддерживаются.
#
# ===========================================
