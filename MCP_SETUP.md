# Настройка MCP серверов для Cursor

## ✅ Что уже настроено

1. **MCP Memory Server** - для глобальной памяти между сессиями
2. **MCP GitHub Server** - для интеграции с GitHub

## 📋 Конфигурация

Файл конфигурации находится в: `C:\Users\Evro\.cursor\mcp.json`

### Текущая конфигурация:

```json
{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": {
        "MEMORY_FILE_PATH": "${env:USERPROFILE}\\.cursor\\memory\\memory.db"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_TOKEN}"
      }
    }
  }
}
```

## 🔧 Настройка GitHub токена

Для работы GitHub MCP сервера необходимо создать Personal Access Token:

### Шаги:

1. **Перейдите на GitHub:**
   - Откройте: https://github.com/settings/tokens
   - Или: Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **Создайте новый токен:**
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Дайте токену имя: `Cursor MCP Server`
   - Выберите срок действия (рекомендуется: 90 дней или No expiration)
   - Выберите необходимые права (scopes):
     - ✅ `repo` - полный доступ к репозиториям
     - ✅ `read:org` - чтение организаций (если нужно)
     - ✅ `read:user` - чтение профиля пользователя
     - ✅ `workflow` - управление workflow (если нужно)

3. **Скопируйте токен** (он показывается только один раз!)

4. **Установите переменную окружения (рекомендуется):**

   **Windows PowerShell:**
   ```powershell
   [System.Environment]::SetEnvironmentVariable('GITHUB_TOKEN', 'ваш_токен_здесь', 'User')
   ```

   **Windows CMD:**
   ```cmd
   setx GITHUB_TOKEN "ваш_токен_здесь"
   ```

   **Или через GUI:**
   - Откройте "Система" → "Дополнительные параметры системы"
   - Нажмите "Переменные среды"
   - В "Переменные пользователя" нажмите "Создать"
   - Имя: `GITHUB_TOKEN`
   - Значение: ваш токен

   **Альтернатива: Прямое указание токена в конфигурации (менее безопасно):**
   
   Если вы не хотите использовать переменную окружения, можете напрямую указать токен в `mcp.json`:
   ```json
   "github": {
     "command": "npx",
     "args": ["-y", "@modelcontextprotocol/server-github"],
     "env": {
       "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_ваш_токен_здесь"
     }
   }
   ```
   
   ⚠️ **Внимание:** Не коммитьте файл `mcp.json` с токеном в Git!

5. **Перезапустите Cursor** для применения изменений

## 🧠 Memory Server

Memory Server автоматически создаст базу данных в:
`C:\Users\Evro\.cursor\memory\memory.db`

Этот сервер позволяет AI запоминать информацию между сессиями через knowledge graph.

### Возможности:
- Создание сущностей (entities)
- Создание связей между сущностями (relations)
- Добавление наблюдений (observations)
- Поиск по графу знаний
- Постоянное хранение между сессиями

## 🔄 Проверка работы

После перезапуска Cursor:

1. **Проверьте Memory Server:**
   - Попросите AI запомнить что-то: "Запомни, что я работаю над проектом InfoBot"
   - В следующей сессии спросите: "Что ты помнишь обо мне?"

2. **Проверьте GitHub Server:**
   - Попросите AI: "Покажи мои репозитории на GitHub"
   - Или: "Создай issue в репозитории InfoBot"

## 📝 Примечания

- MCP серверы загружаются автоматически при запуске Cursor
- Если серверы не работают, проверьте:
  - Установлен ли Node.js (должен быть v14+)
  - Правильно ли установлена переменная GITHUB_TOKEN
  - Перезапущен ли Cursor после изменений
- Memory Server использует локальную базу данных SQLite
- GitHub токен должен иметь необходимые права для операций, которые вы хотите выполнять

## 🛠️ Устранение неполадок

### Memory Server не работает:
- Проверьте, что папка `C:\Users\Evro\.cursor\memory` существует
- Проверьте права доступа к папке

### GitHub Server не работает:
- Проверьте переменную окружения: `echo $env:GITHUB_TOKEN`
- Убедитесь, что токен не истек
- Проверьте права токена на GitHub
- Перезапустите Cursor

### Общие проблемы:
- Убедитесь, что Node.js установлен: `node --version`
- Убедитесь, что npm работает: `npm --version`
- Проверьте логи Cursor для ошибок MCP
